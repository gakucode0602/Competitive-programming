#include<bits/stdc++.h>
using namespace std;

#define out std::cout
#define in std::cin
#define end std::endl
#define ll long long
#define pb push_back
#define PB pop_back
#define For(i,a,b) for(int (i) = (a);(i) < (b);(i)++)
#define rof(i,a,b) for(int (i) = (a);(i) > (b);(i)--)
#define ite(i,c) for(auto &(i) : c)
#define iOS ios_base::sync_with_stdio(false)
#define MOD 1000000007
#define vt vector

// vt<int> a = {1,4,6,9,10};
// int n = a.size();
// int k = 3;
// vt<int> groups(k,0);

// int sum = accumulate(a.begin(),a.end(),0); // thu vien numerics

int n;

void solve(){
	
}

int main(){
	// if(sum % k == 0){

	// }
	// else out << "NO SOLUTION \n";
	// return 0;

	in >> n;
	solve();
}